@model IEnumerable<GCCSI_CO2RE.Models.RD_FacilityTransportCode>
@{
    Layout = null;
}

<div style="text-align:right">
    @Html.ActionLink("Create New", "AddEditFacilityTransportCode", null, new { @id = "createDialog", @class = "btn btn-primary pi-draggable" })
</div>

<div class="datatable-wide" style="padding-left: 0; padding-right: 0">
    <br />
    <div style="width:100%">
        <table id="facTransportGrid" class="table table-striped table-bordered dt-responsive wrap" cellspacing="0">
            <thead>
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.FacilityTransportCodeName)
                    </th>
                    <th>Actions</th>
                </tr>
            </thead>

            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.FacilityTransportCodeName)
                    </td>
                    <td>
                        @Html.ActionLink("Edit", "AddEditFacilityTransportCode", new { id = item.FacilityTransportCodeID }, new { @class = "editDialog" }) |
                        @Html.ActionLink("Details", "FacilityTransportCodeDetails", new { id = item.FacilityTransportCodeID }, new { @class = "detailsDialog" }) |
                        @Html.ActionLink("Delete", "DeleteFacilityTransportCode", new { id = item.FacilityTransportCodeID }, new { @class = "confirmDialog" })
                    </td>
                </tr>
            }

        </table>
    </div>
</div>

<script>
    $('#facTransportGrid').DataTable({
        responsive: true,
        searching: false,
        autoWidth: true
    });
</script>