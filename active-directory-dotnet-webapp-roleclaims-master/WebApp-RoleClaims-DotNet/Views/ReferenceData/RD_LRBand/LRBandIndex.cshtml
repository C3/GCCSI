@model IEnumerable<GCCSI_CO2RE.Models.RD_LRBand>
@{
    Layout = null;
}

<div style="text-align:right">
    @Html.ActionLink("Create New", "AddEditLRBand", null, new { @id = "createDialog", @class = "btn btn-primary pi-draggable" })
</div>

<div class="datatable-wide" style="padding-left: 0; padding-right: 0">
    <br />
    <div style="width:100%">
        <table id="lrBandGrid" class="table table-striped table-bordered dt-responsive wrap" cellspacing="0">
            <thead>
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.BandName)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.BandValue)
                    </th>
                    <th>Actions</th>
                </tr>
            </thead>

            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.BandName)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.BandValue)
                    </td>
                    <td>
                        @Html.ActionLink("Edit", "AddEditLRBand", new { id = item.LRBandID }, new { @class = "editDialog" }) |
                        @Html.ActionLink("Details", "LRBandDetails", new { id = item.LRBandID }, new { @class = "detailsDialog" }) |
                        @Html.ActionLink("Delete", "DeleteLRBand", new { id = item.LRBandID }, new { @class = "confirmDialog" })
                    </td>
                </tr>
            }

        </table>
    </div>
</div>

<script>
    $('#lrBandGrid').DataTable({
        responsive: true,
        searching: false,
        autoWidth: true
    });
</script>