@model IEnumerable<GCCSI_CO2RE.Models.RD_PolicyListOption>
@{
    Layout = null;
}

<div style="text-align:right">
    @Html.ActionLink("Create New", "AddEditPolicyListOption", null, new { @id = "createDialog", @class = "btn btn-primary pi-draggable" })
</div>

<div class="datatable-wide" style="padding-left: 0; padding-right: 0">
    <br />
    <div style="width:100%">
        <table id="polListOption" class="table table-striped table-bordered dt-responsive wrap" cellspacing="0">
            <thead>
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.PolicyListOptionName)
                    </th>
                    <th>Actions</th>
                </tr>
            </thead>

            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.PolicyListOptionName)
                    </td>
                    <td>
                        @Html.ActionLink("Edit", "AddEditPolicyListOption", new { id = item.PolicyListOptionID }, new { @class = "editDialog" }) |
                        @Html.ActionLink("Details", "PolicyListOptionDetails", new { id = item.PolicyListOptionID }, new { @class = "detailsDialog" }) |
                        @Html.ActionLink("Delete", "DeletePolicyListOption", new { id = item.PolicyListOptionID }, new { @class = "confirmDialog" })
                    </td>
                </tr>
            }

        </table>
    </div>
</div>

<script>
    $('#polListOption').DataTable({
        responsive: true,
        searching: false,
        autoWidth: true
    });
</script>