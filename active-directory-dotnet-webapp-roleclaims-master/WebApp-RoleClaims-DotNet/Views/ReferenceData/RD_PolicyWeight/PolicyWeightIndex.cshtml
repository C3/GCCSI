@model IEnumerable<GCCSI_CO2RE.Models.RD_PolicyWeight>
@{
    Layout = null;
}

<div style="text-align:right">
    @Html.ActionLink("Create New", "AddEditPolicyWeight", null, new { @id = "createDialog", @class = "btn btn-primary pi-draggable" })
</div>

<div class="datatable-wide" style="padding-left: 0; padding-right: 0">
    <br />
    <div style="width:100%">
        <table id="policyWeightGrid" class="table table-striped table-bordered dt-responsive wrap" cellspacing="0">
            <thead>
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.PolicyWeightName)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.PolicyWeightPercentage)
                    </th>
                    <th>Actions</th>
                </tr>
            </thead>

            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.PolicyWeightName)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.PolicyWeightPercentage)
                    </td>
                    <td>
                        @Html.ActionLink("Edit", "AddEditPolicyWeight", new { id = item.PolicyWeightID }, new { @class = "editDialog" }) |
                        @Html.ActionLink("Details", "PolicyWeightDetails", new { id = item.PolicyWeightID }, new { @class = "detailsDialog" }) |
                        @Html.ActionLink("Delete", "DeletePolicyWeight", new { id = item.PolicyWeightID }, new { @class = "confirmDialog" })
                    </td>
                </tr>
            }

        </table>
    </div>
</div>

<script>
    $('#policyWeightGrid').DataTable({
        responsive: true,
        searching: false,
        autoWidth: true
    });
</script>