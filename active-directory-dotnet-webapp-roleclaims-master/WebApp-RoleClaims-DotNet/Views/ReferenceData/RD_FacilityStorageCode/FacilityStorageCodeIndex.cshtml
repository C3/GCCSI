@model IEnumerable<GCCSI_CO2RE.Models.RD_FacilityStorageCode>
@{
    Layout = null;
}

<div style="text-align:right">
    @Html.ActionLink("Create New", "AddEditFacilityStorageCode", null, new { @id = "createDialog", @class = "btn btn-primary pi-draggable" })
</div>

<div class="datatable-wide" style="padding-left: 0; padding-right: 0">
    <br />
    <div style="width:100%">
        <table id="facStorageCodeGrid" class="table table-striped table-bordered dt-responsive wrap" cellspacing="0">
            <thead>
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.FacilityStorageCodeName)
                    </th>
                    <th>Actions</th>
                </tr>
            </thead>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.FacilityStorageCodeName)
                    </td>
                    <td>
                        @Html.ActionLink("Edit", "AddEditFacilityStorageCode", new { id = item.FacilityStorageCodeID }, new { @class = "editDialog" }) |
                        @Html.ActionLink("Details", "FacilityStorageCodeDetails", new { id = item.FacilityStorageCodeID }, new { @class = "detailsDialog" }) |
                        @Html.ActionLink("Delete", "DeleteFacilityStorageCode", new { id = item.FacilityStorageCodeID }, new { @class = "confirmDialog" })
                    </td>
                </tr>
            }

        </table>
    </div>
</div>

<script>
    $('#facStorageCodeGrid').DataTable({
        responsive: true,
        searching: false,
        autoWidth: true
    });
</script>